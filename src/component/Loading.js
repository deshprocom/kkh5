import React from 'react'

function Loading() {
    return (
        <div style={{ marginTop: '20px', height: '50px', textAlign: 'center' }}>
            <span>loading...</span>
        </div>
    )
}

export default Loading
